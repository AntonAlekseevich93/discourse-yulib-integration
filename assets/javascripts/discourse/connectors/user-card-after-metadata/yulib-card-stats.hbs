{{!-- 👇👇👇 Пробуем разные пути к данным, чтобы точно поймать статистику 👇👇👇 --}}
{{#let (or this.model.yulib_stats this.args.outletArgs.user.yulib_stats) as |stats|}}
  {{#if stats}}
    <div class="yulib-stats-neutral card-stats">

      {{#if stats.id_done}}
        <span class="y-item">Прочитано: <b>{{stats.id_done}}</b></span>
      {{/if}}

      {{#if stats.id_planned}}
        <span class="y-item">В планах: <b>{{stats.id_planned}}</b></span>
      {{/if}}

      <div class="y-info-wrapper {{if this.showTooltip 'active'}}" {{on "click" this.toggleTooltip}}>
        <span class="y-info-icon">i</span>
        <div class="y-info-popup">
          {{i18n "js.yulib_integration.stats_info_tooltip"}}
        </div>
      </div>

    </div>
  {{else}}
  {{!-- Временная отладка: если данных нет, увидим это в консоли браузера или тут --}}
  {{!-- <div style="display:none;">Data missing for: {{this.model.username}}</div> --}}
  {{/if}}
{{/let}}